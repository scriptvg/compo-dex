---
title: "Pokemon Card"
description: "A Pokémon-inspired card that adapts its surface to the Pokémon's type (and optional secondary type), with support for sizes and an animated Mega variant."
slug: "/components/pokemon-card"
---

<ComponentPreview name="pokemon-card" />

## Installation

<Tabs defaultValue="cli">
<TabsList variant="line">
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<CommandBlock command="shadcn@latest add @compodex/pokemon-card" />

</TabsContent>

<TabsContent value="manual">

<Steps>
<Step>

Install the required dependencies.

<CommandBlock install="class-variance-authority" />

</Step>
<Step>

Add the `badge-type` and `pokemon-type-colors` components that this card depends on.

<CommandBlock command="shadcn@latest add @compodex/badge-type @compodex/pokemon-type-colors" />

</Step>
<Step>

Copy the following code into `components/compodex/ui/pokemon-card.tsx`.

<ComponentSource src="components/compodex/ui/pokemon-card.tsx" />

</Step>
<Step>

Update the import paths to match your project structure.

</Step>
</Steps>

</TabsContent>
</Tabs>

## Usage

```tsx
import {
  PokemonCard,
  PokemonCardHeader,
  PokemonCardTitle,
} from "@/components/compodex/ui/pokemon-card"
```

```tsx
<PokemonCard type="fire">
  <PokemonCardHeader>
    <PokemonCardTitle>Charmander</PokemonCardTitle>
  </PokemonCardHeader>
</PokemonCard>
```

The card surface reads its color from the type-colors theme token (`pokemon-type-colors`). Pass a `secondary` type to get a dual-type gradient surface blending both colors. The `mega` variant adds an animated shimmer overlay for Mega-evolved Pokémon.

## Examples

### Dual type

Pass the `secondary` prop to render a gradient surface that blends the primary and secondary type colors.

<ComponentPreview name="pokemon-card-dual" />

## API Reference

### PokemonCard

Main card container. Also accepts all native `<div>` props.

<PropsTable of="pokemon-card" component="PokemonCard" />

### PokemonCardHeader

Card header slot. Accepts all native `<div>` props.

### PokemonCardTitle

Card title slot. Accepts all native `<div>` props.
