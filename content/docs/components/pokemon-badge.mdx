---
title: "Pokemon Badge"
description: "Badge component to display Pokémon types with consistent color coding."
dependencies: ["class-variance-authority", "radix-ui"]
slug: "/components/pokemon-badge"
---

<ComponentPreview name="pokemon-badge" />

## Installation

<Tabs defaultValue="cli">
<TabsList variant="line">
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<CommandBlock command="shadcn@latest add @compodex/badge-type" />

</TabsContent>

<TabsContent value="manual">

<Steps>
<Step>

Install the required dependencies.

<CommandBlock install="class-variance-authority radix-ui" />

</Step>
<Step>

Copy the following code into `components/compodex/ui/badge-type.tsx`.

<ComponentSource src="components/compodex/ui/badge-type.tsx" />

</Step>
<Step>

Update the import paths to match your project structure.

</Step>
</Steps>

</TabsContent>
</Tabs>

## Usage

```tsx
import { PokemonBadgeType } from "@/components/compodex/ui/badge-type"
```

```tsx
<PokemonBadgeType type="fire">Fire</PokemonBadgeType>
```

## Examples

### Type

Use the `type` prop to display a Pokémon's elemental type. It sets the badge's background and text color automatically based on the type-colors theme token.

<ComponentPreview name="pokemon-badge-variants" />

### Combined

Use the `variant` prop to change the visual style of the badge. Note that some variants may not suit every type due to color contrast.

<ComponentPreview name="pokemon-badge-variants-type" />

## API Reference

Props for `PokemonBadgeType`. Also accepts all native `<span>` props.

<PropsTable of="pokemon-badge" component="PokemonBadgeType" />
