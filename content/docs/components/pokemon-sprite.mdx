---
title: "Pokemon Sprite"
description: "An image element with a fallback for rendering Pokémon sprites. Built on Radix UI Avatar."
slug: "/components/pokemon-sprite"
---

<ComponentPreview name="pokemon-sprite" />

## Installation

<Tabs defaultValue="cli">
<TabsList variant="line">
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<CommandBlock command="shadcn@latest add @compodex/pokemon-sprite" />

</TabsContent>

<TabsContent value="manual">

<Steps>
<Step>

Install the required dependencies.

<CommandBlock install="radix-ui" />

</Step>
<Step>

Copy the following code into `components/compodex/ui/pokemon-sprite.tsx`.

<ComponentSource src="components/compodex/ui/pokemon-sprite.tsx" />

</Step>
<Step>

Update the import paths to match your project structure.

</Step>
</Steps>

</TabsContent>
</Tabs>

## Usage

```tsx
import {
  PokemonSprite,
  PokemonSpriteImage,
  PokemonSpriteFallback,
  PokemonSpriteGroup,
  PokemonSpriteGroupCount,
} from "@/components/compodex/ui/pokemon-sprite"
```

```tsx
<PokemonSprite>
  <PokemonSpriteImage src="/sprites/pikachu.png" alt="Pikachu" />
  <PokemonSpriteFallback>PK</PokemonSpriteFallback>
</PokemonSprite>
```

`PokemonSprite` is built on [Radix UI Avatar](https://www.radix-ui.com/primitives/docs/components/avatar) and inherits its image-load lifecycle: `PokemonSpriteImage` renders once the image loads, and `PokemonSpriteFallback` is shown while it loads or if it fails.

### Size scale

The `size` prop controls the square dimensions of the sprite. Use `card` when placing a sprite inside a `PokemonCard` media slot — it is sized to fit that context precisely.

| Value | Size |
| --- | --- |
| `xs` | 20 px |
| `sm` | 24 px |
| `default` | 32 px |
| `md` | 40 px |
| `lg` | 80 px |
| `xl` | 112 px |
| `2xl` | 144 px |
| `card` | 160 px |

```tsx
<PokemonSprite size="lg">
  <PokemonSpriteImage src="/sprites/charizard.png" alt="Charizard" />
  <PokemonSpriteFallback>CH</PokemonSpriteFallback>
</PokemonSprite>
```

## Examples

### Sprite group

<ComponentPreview name="pokemon-sprite-group" />

## API Reference

### PokemonSprite

Root sprite container — wraps [Radix UI `Avatar.Root`](https://www.radix-ui.com/primitives/docs/components/avatar). Accepts all `Avatar.Root` props plus `size`.

<PropsTable of="pokemon-sprite" component="PokemonSprite" />

### Subcomponents

| Component | Base | Description |
| --- | --- | --- |
| `PokemonSpriteImage` | `Avatar.Image` | Renders the Pokémon image. Accepts all `Avatar.Image` props. |
| `PokemonSpriteFallback` | `Avatar.Fallback` | Shown while the image loads or if it fails. Accepts all `Avatar.Fallback` props. |
| `PokemonSpriteBadge` | `<span>` | A badge positioned over the sprite (bottom-right corner). Accepts all `<span>` props. |
| `PokemonSpriteGroup` | `<div>` | Groups multiple sprites with overlap. Accepts all `<div>` props. |
| `PokemonSpriteGroupCount` | `<div>` | Displays a remaining count chip inside a group. Accepts all `<div>` props. |
