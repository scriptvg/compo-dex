# Installation

Guía rápida para instalar y arrancar **compodex** en local.

## Requisitos

- Node.js 18.18 o superior.
- [pnpm](https://pnpm.io) como gestor de paquetes.

## Instalar dependencias

```bash
pnpm install
```

## Arrancar en desarrollo

```bash
pnpm dev
```

El servidor de desarrollo usa Turbopack y queda disponible en
`http://localhost:3000`.

## Scripts disponibles

- `pnpm dev` — servidor de desarrollo.
- `pnpm build` — build de producción.
- `pnpm start` — sirve el build de producción.
- `pnpm lint` — analiza el código con ESLint.
- `pnpm typecheck` — comprueba los tipos con TypeScript.

## Siguiente paso

Explora los [componentes](/docs/components/pokemon-badge) disponibles.
